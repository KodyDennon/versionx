---
title: versionx (CLI root)
description: Auto-generated CLI reference for the versionx root command and its subcommands.
sidebar_position: 1
---

{/* GENERATED by `cargo xtask docs --only cli`. Do not edit by hand. */}

# `versionx`

_Generated from `versionx --help-json` at version 0.1.0._

Cross-platform, cross-language, cross-package-manager version manager.

## Global flags

| Flag | Value | Default | Required | Global | Description |
|---|---|---|---|---|---|
| `--output` | `<OUTPUT>` | `human` |  | yes | Output format. `human` is the colored pretty default; `json` emits a single JSON object; `ndjson` streams newline-delimited events suitable for AI agents and shell pipelines. Possible values: `human`, `json`, `ndjson`. |
| `-q` / `--quiet` | `<QUIET>` |  |  | yes | Suppress all output except errors. Possible values: `true`, `false`. |
| `-v` / `--verbose` | `<VERBOSE>` |  |  | yes | Increase output verbosity (`-v`, `-vv`, `-vvv`) |
| `--cwd` | `<CWD>` |  |  | yes | Working directory for the command. Defaults to the current directory |
| `--no-daemon` | `<NO_DAEMON>` |  |  | yes | Bypass the daemon and run every operation in-process (slower). Possible values: `true`, `false`. |

## Subcommands

| Command | Summary |
|---|---|
| [`activate`](./activate) | Emit a shell integration hook. Put `eval "$(versionx activate bash)"` in your shell rc file to start the daemon on login and prepend the shims dir to `PATH` |
| [`ai`](./ai) | BYO-API-key LLM configuration + smoke test |
| [`bump`](./bump) | Propose version bumps for every dirty component (+ cascade). This is a preview of the 0.4 release orchestration: the plan is printed but nothing is written |
| [`changelog`](./changelog) | Changelog drafting (voice-aware, via BYO-API-key) |
| [`changeset`](./changeset) | Changeset workflow (release-please-style metadata files) |
| [`daemon`](./daemon) | Start or manage the `versiond` background daemon |
| [`doctor`](./doctor) | Diagnose the local Versionx install + workspace health. Prints a structured pass/fail per check |
| [`exec`](./exec) | Run a command using the workspace's pinned tool resolution. `versionx exec node app.js` invokes the pinned Node |
| [`fleet`](./fleet) | Fleet-level orchestration across member repositories |
| [`global`](./global) | User-level default pins (falls back when no versionx.toml pins a tool) |
| [`import`](./import) | Import an existing repo's tool config (mise/asdf/nvm/poetry) into a fresh `versionx.toml` |
| [`init`](./init) | Synthesize a `versionx.toml` from detected ecosystems and write it to the repo root. Safe to re-run |
| [`install`](./install) | Install a toolchain globally (outside a repo context). Mirrors `mise install &lt;tool&gt; &lt;version&gt;` for users migrating from mise/asdf |
| [`install-shell-hook`](./install-shell-hook) | Auto-install the activation hook into the user's shell rc file (~/.zshrc, ~/.bashrc, etc.). Idempotent: re-running detects the existing marker + skips |
| [`links`](./links) | Manage external links (submodule / subtree / virtual / ref) |
| [`mcp`](./mcp) | MCP server for AI-agent integration |
| [`plan`](./plan) | Manage stored release plans under `.versionx/plans/` |
| [`policy`](./policy) | Policy authoring and evaluation |
| [`release`](./release) | Release orchestration subcommands (plan/apply, publish, snapshots) |
| [`runtime`](./runtime) | Runtime installation and management subcommands |
| [`self-check`](./self-check) | One-shot self-test: doctor + verify + a no-op sync plan |
| [`state`](./state) | State backup / restore / repair via refs/versionx/history |
| [`status`](./status) | Print a summary of the current workspace: detected ecosystems, pinned runtimes, outstanding changes, and policy findings |
| [`sync`](./sync) | Install everything declared in `versionx.toml` + `versionx.lock`: toolchains, package managers, and dependencies |
| [`tui`](./tui) | Launch the interactive terminal dashboard |
| [`verify`](./verify) | Verify that the lockfile matches the current manifest state. Designed for CI as a fast fail-closed integrity check |
| [`waiver`](./waiver) | Waiver management — time-boxed policy exceptions |
| [`which`](./which) | Show which binary the shim resolves to in the current context and why. Debugging aid for `PATH` / shim behavior |
| [`workspace`](./workspace) | Inspect your workspace — components, change state, dependency graph |

## See also

- [`versionx.toml` reference](../versionx-toml)
- [Environment variables](../environment-variables)
- [Exit codes](../exit-codes)
