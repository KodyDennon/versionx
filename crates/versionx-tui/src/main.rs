//! Interactive ratatui dashboard (launched by `versionx tui`).

#![deny(unsafe_code)]

fn main() {
    println!("versionx-tui v{} — scaffolded, not yet implemented.", env!("CARGO_PKG_VERSION"));
    println!("See docs/spec/ for the full vision specification.");
    std::process::exit(0);
}
