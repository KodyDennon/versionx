//! The `versiond` long-running daemon with JSON-RPC 2.0 over UDS / named pipe.

#![deny(unsafe_code)]

fn main() {
    println!("versiond v{} — scaffolded, not yet implemented.", env!("CARGO_PKG_VERSION"));
    println!("See docs/spec/ for the full vision specification.");
    std::process::exit(0);
}
