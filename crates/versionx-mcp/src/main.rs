//! MCP server over stdio + loopback HTTP, built on the official rmcp SDK.

#![deny(unsafe_code)]

fn main() {
    println!("versionx-mcp v{} — scaffolded, not yet implemented.", env!("CARGO_PKG_VERSION"));
    println!("See docs/spec/ for the full vision specification.");
    std::process::exit(0);
}
