//! Local-only axum web UI (loopback in v1.0).

#![deny(unsafe_code)]

fn main() {
    println!("versionx-web v{} — scaffolded, not yet implemented.", env!("CARGO_PKG_VERSION"));
    println!("See docs/spec/ for the full vision specification.");
    std::process::exit(0);
}
